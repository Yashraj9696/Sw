Skip to content
Navigation Menu
Yashraj9696
Sw

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Sketch Art Video Processing
Create 6.yml #1
Jobs
Run details
Workflow file for this run
.github/workflows/6.yml at c0f92d7
name: Sketch Art Video Processing

on: [push, pull_request, workflow_dispatch]

jobs:
  process_video:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip  
          pip install -r requirements.txt  
          pip install opencv-python-headless numpy moviepy requests
      - name: Download Video from URL
        run: |
          VIDEO_URL="https://dl.springsfern.in/play/1386449?hash=444d2c"  
          curl -L "$VIDEO_URL" -o input.mp4
          if [ ! -s input.mp4 ]; then
            echo "ERROR: Downloaded video is missing or empty!"
            exit 1
          fi
      - name: Run Sketch Effect Script
        run: python sketch_effect.py

      - name: Check Output File
        run: |
          if [ ! -f "output_sketch.mp4" ]; then
            echo "ERROR: No output video found!"
            exit 1
          fi
      - name: Upload Processed Video as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: output-video
          path: output_sketch.mp4
Create 6.yml · Yashraj9696/Sw@c0f92d7
